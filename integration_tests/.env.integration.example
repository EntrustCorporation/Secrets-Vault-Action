# Common settings
TEST_BASE_URL=https://your-vault-api.example.com
TEST_BOX_ID=your-box-id
TEST_SECRET_ID=your-secret-id
TEST_CA_CERT=base64-encoded-ca-cert-if-needed

# Authentication type (token or userpass)
TEST_AUTH_TYPE=token

# Token auth settings
TEST_API_TOKEN=your-api-token

# UserPass auth settings (only needed if TEST_AUTH_TYPE=userpass)
# TEST_USERNAME=your-username
# TEST_PASSWORD=your-password
# TEST_VAULT_UID=your-vault-uid
